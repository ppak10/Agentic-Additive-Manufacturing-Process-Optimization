{
  "name": "agentic-sls",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "services/plugins/.opencode/plugins/agentic-sls.ts",
  "scripts": {
    "dev:server": "npm --prefix services/recorder run dev",
    "dev:web": "npm --prefix services/web run dev",
    "typecheck": "npm --prefix services/recorder run typecheck && npm --prefix services/web run typecheck && npm --prefix services/plugins/mcp run typecheck && npm --prefix services/harness run typecheck",
    "smoke": "npm --prefix services/plugins/mcp run smoke"
  }
}
